# Jac build cache + persisted graph data (runtime artifacts)
.jac/
jac.local.toml

# Node / client build
node_modules/

# Env / secrets
.env

# Python
__pycache__/
*.pyc

# Editors
.vscode/
.idea/

# Local git worktrees (other tracks)
.claude/worktrees/

# Bridge deps (bridge/ is deployed separately, e.g. Render)
bridge/node_modules/
